# Copy to .env in this folder. Values are NOT in git — get them from
# Vercel (tlc-mvp-server) or the Hasura / Brevo consoles.
# See the root README "Credentials / how to log in".

HASURA_DB_URL=
HASURA_ADMIN_SECRET=
JWT_SECRET_KEY=
CRYPTO_TICKET=
MAIL_API_KEY=
